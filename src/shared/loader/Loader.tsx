import React from 'react';

import { LoaderContainer } from '@/shared/loader/Loader.styled';

import type { Component } from '@/types';

const Loader: Component = () => (
  <LoaderContainer>
    <div />
  </LoaderContainer>
);

export default Loader;
